<!DOCTYPE html> <html> <head> <title>Puzzle Game</title> <style> body{text-align:center;font-family:Arial;} .grid{width:300px;margin:auto;display:grid;grid-template-columns:repeat(3,100px);gap:5px;} .tile{height:100px;font-size:30px;background:#2196F3;color:white;border:none;} .empty{background:#ccc;} </style> </head> <body>  <h2>Puzzle Game</h2> <p>Arrange numbers in correct order</p>  <div class="grid" id="grid"></div>  <script> let tiles=[1,2,3,4,5,6,7,8,""]; function draw(){  grid.innerHTML="";  tiles.forEach((n,i)=>{   let b=document.createElement("button");   b.className="tile";   if(n==="")b.classList.add("empty");   b.innerText=n;   b.onclick=()=>move(i);   grid.appendChild(b);  }); } function move(i){  let e=tiles.indexOf("");  let v=[i-1,i+1,i-3,i+3];  if(v.includes(e)){   [tiles[i],tiles[e]]=[tiles[e],tiles[i]];   draw();  } } draw(); </script>  </body> </html>
